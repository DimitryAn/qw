#include <iostream>
#include <algorithm>
#include <vector>
using namespace std;

int main()
{
    /*
    double n, k, count = 0, sum = 0;
    cin >> n >> k;
    vector<double> numbers(n);
    for (int i = 0; i < n; ++i)
    {
        cin >> numbers[i];
    }
    
    for (int i = 0; i < n; ++i)
    {
        if (numbers[i] == k)
        {
            ++count;
            continue;
        }
        sum = numbers[i];
        for (int j = (i + 1); j < n; ++j)
        {
            sum += numbers[j];
            if (sum == k)
            {
                ++count;
                int size = n + 1;
                j = size;
                continue;
            }
        }
    }
    cout << count << endl;*/
    for ( int i  = 0 ; i < 100000; ++i )
    {
        cout << 100000000 << endl;
    }
    return 0;
}